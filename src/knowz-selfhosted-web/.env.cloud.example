# Copy this to .env.cloud (gitignored) to enable npm run dev:cloud
# This points the Vite dev server proxy at the deployed rg-knowz-sh Container App API
# instead of a local API, so you can run just the web client against live cloud data.

VITE_PROXY_TARGET=https://knowz-sh-api.jollymeadow-44a9327c.eastus2.azurecontainerapps.io
